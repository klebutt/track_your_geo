# Worklog — 2026-06-28

## Shipped today

1. **multi-llm-citations** — OpenAI + Perplexity + Gemini probe fan-out; per-row `model_name`; provider-specific citation extraction; cost aggregation across models.
2. **multi-brand-yaml** — Demo brands moved to `apps/api/pilots/demo/*.yaml`; recursive YAML loading; removed `hardcoded_pilots.py`.
3. **Async runs + resilience** — `POST /api/runs` returns immediately; probes run in background; UI polls for progress; per-model failures no longer abort the whole run; LiteLLM dict cost parsing fixed.

Both OpenSpec changes archived under `openspec/changes/archive/2026-06-28-*`.

## Multi-LLM citations — deployment validation

Post-deploy smoke test after setting `TYGEO_ENABLED_PROBES`, `PERPLEXITY_API_KEY`, and `GEMINI_API_KEY` in `.env` and Railway.

### Production API (Railway)

| Check | Result |
|--------|--------|
| Health | `GET /api/health` → `{"status":"ok"}` |
| Pilots | 3 demo brands returned |
| Multi-model run | `POST /api/runs` with `dishoom-london` completed in ~4 min |
| Query fan-out | **30 rows** = 10 queries × 3 models |
| Models on run | `gpt-4o-mini-search-preview,perplexity/sonar-pro,gemini/gemini-2.5-flash` |
| Per-row `model_name` | 10 rows each for all 3 providers |
| Citations | **30/30 rows** have `cited_domains` |
| Cost aggregation | Row costs sum to **$0.5674** = `total_cost_usd` |
| Visibility | 47% (14/30 answers mentioned "Dishoom") |

### Local (.env)

| Check | Result |
|--------|--------|
| Config | 3 models loaded from `TYGEO_ENABLED_PROBES` |
| Per-model probes | 3/3 succeeded (OpenAI, Perplexity, Gemini) |
| `execute_run` pipeline | 1 query → 3 `query_results`, cost aggregated correctly |
| Unit tests | **22/22 passed** |

### Frontend (Vercel)

- https://track-your-geo.vercel.app/ loads and shows the dashboard UI.

---

## Citation behavior (expected quirks)

**Perplexity** is working well — avg **8.4 domains** per probe (e.g. `theinfatuation.com`, `tripadvisor.com`, `reddit.com`).

**OpenAI** mostly cites `google.com` because search annotations point at Google Maps URLs. That is the existing OpenAI path, not a regression.

**Gemini** returns `vertexaisearch.cloud.google.com` — LiteLLM's grounding metadata uses Google redirect URLs rather than final source domains. Citations are being captured, but domain quality is weaker than Perplexity. Improving that would be a follow-up extraction tweak, not a deploy issue.

---

## Production run to inspect

Run **#1** on Railway:

- API: `GET https://trackyourgeo-production.up.railway.app/api/runs/1`
- Dashboard: https://track-your-geo.vercel.app/ — Model column and multiple rows per query visible after run.

That production run cost about **$0.57** for the full Dishoom pilot across all three models.

---

## Multi-brand YAML — notes

- Pilot files live at **`apps/api/pilots/`** (resolved relative to the API package root so Railway deploys from `apps/api` include them).
- Add a customer brand: drop a `.yaml` under `apps/api/pilots/customers/` (or any subfolder). Quote query strings that contain `: ` to avoid YAML parsing issues.
- `GET /api/pilots` must return demo brands after deploy; if empty, confirm `TYGEO_PILOT_DIR=pilots` and that YAML files ship with the API root.

---

## Async runs + production fixes (afternoon)

### Problem

Full pilot runs (10 queries × 3 models = **30 probes**) take **~4–8 minutes**. The original synchronous `POST /api/runs` held the HTTP connection open with no response bytes, causing **“Failed to fetch”** in the browser after ~2 minutes.

A later failure mode: a single **Gemini** error (e.g. free-tier **429 rate limit**) aborted the entire run and surfaced as *“Analysis run failed on the server.”*

### Solution

| Layer | Behavior |
|--------|----------|
| API | `POST /api/runs` creates run with `status: running`, returns immediately, probes in `BackgroundTasks` |
| API | Each successful probe commits to SQLite incrementally (poll shows growing `query_results`) |
| API | Per-model probe errors logged in `usage_log` (`phase: probe_error`); run still **completes** with partial results |
| API | LiteLLM `completion_cost` dict breakdown normalized via `total_cost` |
| UI | Polls `GET /api/runs/{id}` every 3s; button shows `Running probes… (N complete)` |
| UI | Warns when probe errors present (e.g. Gemini quota) |

### Validated on production (SDL Surveying)

- Run completed with **24/30** probes (OpenAI + Perplexity); **6 Gemini failures** as expected on free tier.
- User plan: move Gemini to paid tier when ready, or temporarily omit from `TYGEO_ENABLED_PROBES`.

### Operational notes

- **Gemini free tier:** ~20 requests/min for `gemini-2.5-flash` — easy to hit on 30-probe runs. Symptoms: `probe_error` entries in `usage_log`, missing Gemini rows in results table.
- **Workaround:** `TYGEO_ENABLED_PROBES=gpt-4o-mini-search-preview,perplexity/sonar-pro` until billing enabled.
- **Run cost:** ~$0.50–0.60 per full 3-model pilot (varies by brand/queries).

---

## Bottom line

Production is stable for multi-model GEO probing with YAML pilots, dashboard history, persistent run storage on Railway, structured extraction, and enriched recommendations. Expect **partial Gemini results** on free tier until billing is enabled.

### Repeat checks

Smoke scripts under `eval/`:

- `eval/smoke_multi_llm.py` — one probe per enabled model
- `eval/smoke_execute_run.py` — full `execute_run` with a single-query pilot

Unit tests: `pytest eval -q` (45 tests as of this session).

---

## structured-extraction — shipped

- **Gate:** case-insensitive substring still sets `brand_mentioned` / visibility rate.
- **Extraction:** when visible, `gpt-4o-mini` JSON call records `sentiment`, `mention_position`, `relevance_score` on each `query_results` row; cost aggregated into run total and logged as `phase: extraction` in `usage_log`.
- **Score:** composite score uses weighted formula (40% visibility, 30% position, 20% sentiment, 10% citations) — see `apps/api/docs/geo-scoring-formula.md`.
- **UI:** Sentiment (color chips) and Position columns in query table; prod verified on Vercel.
- **Tests:** `eval/test_extraction.py`; `pytest eval -q` 39 passed.
- **OpenSpec:** archived at `openspec/changes/archive/2026-06-28-structured-extraction/`

---

## dashboard-history-view — shipped

- **API:** `GET /api/runs?pilot_id=…&limit=N` filters by pilot; `RunListItem` includes `status`.
- **UI:** On brand select, auto-loads latest completed run; `TrendChart` (recharts) plots visibility % over time.
- **UX:** Summary §2 (with last-probe timestamp), trend §3, neutral probes §4.
- **Persistence:** Railway volume at `/data` + `TYGEO_DATABASE_URL=sqlite:////data/tygeo.db` verified (SDL + Dishoom runs survive redeploy).
- **Tests:** `eval/test_api_runs.py::test_list_runs_filters_by_pilot_id`; `pytest eval -q` 30 passed.
- **Archive:** `openspec/changes/archive/2026-06-28-dashboard-history-view/`

---

## recommendations-engine-v1 — shipped

- **Backend:** `execute_run` now computes `sentiment_summary`, `avg_position`, and `citation_gaps`, then calls enriched `run_recommendations` (~$0.005/run). Results persist to `recommendations` table; failures logged as `recommendations_error` without failing the run.
- **Prompt:** Includes composite score, sentiment breakdown, position average, citation gaps, weak queries, and competitor wins; categories: `website`, `third_party`, `pr_comms`, `technical`.
- **UI:** New §4 **Insights & optimization** (between trend chart and neutral probes) with `ScoreBreakdown` (40/30/20/10 bars) and `RecommendationsList` cards (impact badge + category).
- **Tests:** `eval/test_recommendations.py`; `pytest eval -q` 45 passed; frontend `npm run build` OK.
- **Prod validation:** Dishoom pilot run confirmed — recommendations reference real domains (e.g. The Infatuation) and named competitors; score breakdown and cards render correctly in dashboard §4.
- **OpenSpec:** archived at `openspec/changes/archive/2026-06-28-recommendations-engine-v1/`
