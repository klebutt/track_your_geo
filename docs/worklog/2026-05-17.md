# Worklog — 17 May 2026

## Summary

### OpenSpec: `add-citation-domains`

- Added change artifacts: `proposal.md`, `design.md`, `specs/geo-probe/spec.md`, `tasks.md`.
- Updated canonical `openspec/specs/geo-probe/spec.md` with citation requirements.

### Per-query citation domains (commercial)

- New `apps/api/tygeo/citations.py`: extract domains from HTTP(S) URLs and markdown links; classify `brand_owned` vs `third_party` via pilot `brand_domains` and brand-slug heuristic.
- `query_results.cited_domains` JSON column with SQLite migration on startup.
- Pilots: `brand_domains` on Dishoom (`dishoom.com`) and Clio (`clio.com`).
- Dashboard: **Sources (domains)** column and chips under full replies; removed illustrative `seed_domains` panel; disclaimer mentions parsed domains.
- Tests: `eval/test_citations.py`.

### Validation

- `pytest eval -q` — pass.

### Web search probes (citations fix)

- Probes now use **`gpt-4o-mini-search-preview`** + `web_search_options` (not plain `gpt-4o-mini` chat).
- Citations from OpenAI **`url_citation` annotations**; text URL parsing as fallback.
- Config: `TYGEO_PROBE_MODEL`, `TYGEO_SEARCH_CONTEXT_SIZE` (default `low`).
- UI/disclaimer updated for web-search path.

## Suggested next actions

- Archive OpenSpec change `add-citation-domains` when satisfied.
- Manual smoke: **new run** on Dishoom — expect multiple domains per query; higher cost than before.
